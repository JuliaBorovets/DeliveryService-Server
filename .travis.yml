language: java
jdk: openjdk8

dist: trusty

cache:
  directories:
    - $HOME/.m2

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email:
    recipients:
      - juliaborovets2001@gmail.com
    on_success: always
    on_failure: always

before_install:
  - chmod +x mvnw
